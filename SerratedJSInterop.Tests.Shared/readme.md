# SerratedJSInterop.Tests.Shared

Test cases for SerratedJSInterop (e.g. DOM manipulation, events, traversal). Referenced by the Blazor and WasmBrowser test hosts so the same tests run on each platform.
